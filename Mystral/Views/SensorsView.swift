import SwiftUI

struct SensorsView: View {
    let fanController: FanController

    var body: some View {
        Text("Sensors")
            .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
